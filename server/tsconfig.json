{
  "compilerOptions": {
    // ── Output ────────────────────────────────────────────────────────────────
    "rootDir": "./src",
    "outDir": "./dist",

    // ── Module system ─────────────────────────────────────────────────────────
    // CommonJS is required here because:
    //   1. package.json has "type": "commonjs"
    //   2. ts-node (non-ESM) requires CommonJS
    //   3. "nodenext" implicitly turns on verbatimModuleSyntax which breaks ts-node
    "module": "commonjs",
    "moduleResolution": "node",
    "target": "es2020",

    // ── Emit ──────────────────────────────────────────────────────────────────
    "sourceMap": true,
    "declaration": true,
    "declarationMap": true,
    "esModuleInterop": true,
    "allowSyntheticDefaultImports": true,

    // ── Type-checking ─────────────────────────────────────────────────────────
    "strict": true,
    "skipLibCheck": true,
    "isolatedModules": true,

    // verbatimModuleSyntax is intentionally OFF:
    // It is only meaningful with ESM ("module": "nodenext" / "esnext").
    // With CommonJS it causes TS1295 errors on every type-only import.
    "verbatimModuleSyntax": false,

    // ── Runtime types ─────────────────────────────────────────────────────────
    "lib": ["es2020"],
    "types": ["node"]
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist"]
}
